# Optional: cap rows while testing huge files (e.g. 100000)
# SEMANTIC_SEARCH_MAX_ROWS=

# Optional: PDP-style link prefix (slug from English Name). Example H&M UAE PDP pattern:
# NEXT_PUBLIC_PRODUCT_URL_BASE=https://ae.hm.com/en/buy
